# fintech-etl-pipeline
Scenario: You work at a fintech company. The mobile app emits event data as JSON whenever a user performs a financial action. Your job is to build an ETL pipeline that flattens these events into analytics-ready tables.
